## 📊 Текущий статус проекта и План разработки (TODO)

### ✅ Что уже сделано и настроено:

#### 1. Глобальная архитектура и Системные узлы
* [x] **Рефакторинг каркаса:** Главный макет (`MainLayout`) и боковое меню (`NavMenu`) полностью переведены на сетку и компоненты дизайн-системы *Radzen UI*.
* [x] **Отказоустойчивость сессии:** Системное окно потери связи с сервером (`ReconnectModal`) стилизовано под Radzen, очищено от JS-ошибок инициализации и стабильно открывается даже при полном падении бэкенда.
* [x] **Страницы ошибок:** Созданы и стилизованы кастомные страницы для критических сбоев (`Error`) и несуществующих маршрутов (`NotFound`).

#### 2. Международная локализация (7 языков)
* [x] **Архитектура мультиязычности:** В `Program.cs` настроен middleware локализации на базе `.resx` файлов. Базовый язык — **English (en)**.
* [x] **Переключатель на лету:** В шапку встроен ультра-компактный `RadzenDropDown` с отображением коротких кодов (**EN, RU, DE, FR, TH, VI, JA**). 
* [x] **Cookie-провайдер:** Реализован Minimal API эндпоинт `/Culture/SetCulture` для мгновенной записи выбора пользователя в Cookie и бесшовного обновления интерфейса.
* [x] **Наполнение переводов:** Полностью локализованы и заполнены тексты для главного дашборда (`Home`), страницы описания (`About`) и бокового меню.

#### 3. Модуль Water Sort Solver (Сортировка воды)
* [x] **Компонентная изоляция:** Логика и интерфейс полностью вынесены из страниц в изолированную фичу `Features/WaterSort` с разделением на Code-Behind (`.razor` и `.razor.cs`).
* [x] **Интерактивный редактор:** Реализована настройка количества колб (от 2 до 14) с бережным сохранением уже выставленных пользователем цветов при изменении структуры доски.
* [x] **Медиаплеер анимации:** Написан надежный алгоритм управления автопроигрыванием (**Старт ▷, Пауза ⏸, Стоп ⏹**) с защитой от зависаний через `CancellationToken` и корректной блокировкой кнопок при завершении шагов.
* [x] **Интерактивный таймлайн:** Вывод пошагового руководства переливания жидкостей переведен в компонент `RadzenTimeline` с поддержкой ручной покликовой перемотки состояний доски.
* [x] **Синхронизация алгоритма:** Исправлены баги сжатия пустых уровней `"#eeeeee"` и скорректирован метод `IsSolved()` в классах `WaterSolver` и `WaterSortState` под реальные сценарии сборки.

---

### 📝 TODO (План на следующие этапы):

#### 🟩 Шаг 1. Модуль Mahjong / Triple Tile Solver
* [ ] Вынести код страницы `MahjongSolver.razor` в изолированную фичу `Features/Mahjong` с использованием паттерна Code-Behind.
* [ ] Избавить разметку редактора свойств плиток от старых Bootstrap-классов (`mb-3`, `w-100`), переведя её на сетку `RadzenRow/Column` и вертикальные контейнеры `RadzenStack`.
* [ ] Внедрить `IStringLocalizer` и создать файлы ресурсов для перевода редактора Маджонга на все 7 поддерживаемых языков.
* [ ] Разработать для Маджонга визуальный пошаговый плеер найденных решений (разбора пар костей) по аналогии с плеером сортировки воды.

#### 🟨 Шаг 2. Справочник Базы Маджонга (`mahjong`)
* [ ] Настроить вывод доступных категорий, типов и рисунков костей на странице `mahjong` с использованием адаптивных таблиц данных `RadzenDataGrid`.
* [ ] Добавить локализацию для всех элементов справочника и фильтров поиска.

#### 🟦 Шаг 3. Полировка и дополнительные фичи
* [ ] Интегрировать всплывающие системные уведомления `NotificationService` от Radzen для вывода ошибок (например, если у головоломки нет решений).
* [ ] Реализовать сохранение истории последних решенных раскладок в локальное хранилище браузера (LocalStorage) для вывода краткой статистики на главной странице.
